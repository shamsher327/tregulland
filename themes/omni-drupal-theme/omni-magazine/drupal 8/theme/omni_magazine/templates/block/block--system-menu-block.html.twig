{#
/* COPYRIGHT
------------------------------------------------------------------  
  Omni Magazine for Drupal 8.x - Version 1.0                           
  Copyright (C) 2016 esors.com All Rights Reserved.           
  @license - Copyrighted Commercial Software                   
------------------------------------------------------------------  
  Theme Name: Omni Magazine                                            
  Author: ESORS                                           
  Date: 15th February 2016                                        
  Website: http://www.esors.com/                              
------------------------------------------------------------------  
  This file may not be redistributed in whole or   
  significant part.                                            
----------------------------------------------------------------*/
#}
{% set region %}{{ block_region }}{% endset %}
{% set heading_id = attributes.id ~ '-menu'|clean_id %}

{% if region == 'quick_link' or  region == 'secondary_menu' or  region == 'closure' %}
  <nav role="navigation" aria-labelledby="{{ heading_id }}"{{ attributes|without('role', 'aria-labelledby') }}>
    {# Label. If not displayed, we still provide it for screen readers. #}
    {% if not configuration.label_display %}
      {% set title_attributes = title_attributes.addClass('visually-hidden') %}
    {% endif %}
    {{ title_prefix }}
    <h3{{ title_attributes.addClass('title') }}>{{ configuration.label }}</h3>
    {{ title_suffix }}
    {{ content }}    
  </nav>
{% else %}
  <div class="block block-system-menu">
    <div class="inner">
      {# Label. If not displayed, we still provide it for screen readers. #}
      {% if not configuration.label_display %}
        {% set title_attributes = title_attributes.addClass('visually-hidden') %}
      {% endif %}
      {{ title_prefix }}
      <h3{{ title_attributes.addClass('title') }}>{{ configuration.label }}</h3>
      {{ title_suffix }}
      <div class="content clearfix">
        <nav role="navigation" aria-labelledby="{{ heading_id }}"{{ attributes|without('role', 'aria-labelledby') }}>
          {{ content }}          
        </nav>
      </div>
    </div>
  </div>
{% endif %}














































